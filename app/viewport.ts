// app/viewport.ts
import type { Viewport } from "next"

/**
 * Controls mobile browser UI colors, etc.
 * Moved from metadata.themeColor to match Next.js guidance.
 */
export const viewport: Viewport = {
  themeColor: "#0B0F1A",
}
